
<!-- Login Section -->
<!-- Header Section - Redesigned with Rails helpers -->
<header class="header">
  <div class="header-container">
    <div class="logo">
      <div class="logo-icon">R</div>
      <div class="logo-text">RENATA</div>
    </div>

    <nav>
      <ul class="nav-menu">
        <li><%= link_to "Home", root_path %></li>
        <li><%= link_to "About", "#" %></li>
        <li><%= link_to "Services", "#" %></li>
        <li><%= link_to "Resources", "#" %></li>
        <li><%= link_to "Contact", "#" %></li>
      </ul>
    </nav>

    <div class="header-actions">
      <%= link_to "Log In", new_user_session_path, class: "btn-header btn-login" %>
      <%= link_to "Sign Up", new_user_registration_path, class: "btn-header btn-signup" %>
    </div>
  </div>
</header>

<!-- Main Content -->
<main class="main-content">
  <div class="login-container">
    <div class="login-box">
      <div class="auth-card" id="authCard">
        <div class="hover-glow" id="hoverGlow"></div>
        <h2 class="login-title">Sign in</h2>

        <!-- Social Buttons -->
        <div class="social-container">
          <!-- Instagram -->
          <a href="#" class="circle" aria-label="Instagram">
            <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
              <path d="M7.75 2h8.5A5.75 5.75 0 0122 7.75v8.5A5.75 5.75 0 0116.25 22h-8.5A5.75 5.75 0 012 16.25v-8.5A5.75 5.75 0 017.75 2zm0 1.5A4.25 4.25 0 003.5 7.75v8.5A4.25 4.25 0 007.75 20.5h8.5a4.25 4.25 0 004.25-4.25v-8.5A4.25 4.25 0 0016.25 3.5h-8.5zM12 7a5 5 0 110 10 5 5 0 010-10zm0 1.5a3.5 3.5 0 100 7 3.5 3.5 0 000-7zm4.75-.88a1.13 1.13 0 11-2.25 0 1.13 1.13 0 012.25 0z"></path>
            </svg>
          </a>
          <!-- LinkedIn -->
          <a href="#" class="circle" aria-label="LinkedIn">
            <svg viewBox="0 0 448 512" xmlns="http://www.w3.org/2000/svg">
              <path d="M100.28 448H7.4V148.9h92.88zM53.79 108.1a53.79 53.79 0 1 1 53.79-53.8 53.79 53.79 0 0 1-53.79 53.8zM447.9 448h-92.4V304.1c0-34.3-12.3-57.7-43.1-57.7-23.5 0-37.6 15.8-43.8 31.1-2.3 5.4-2.9 12.9-2.9 20.4V448h-92.4s1.2-270.5 0-298.9h92.4v42.3c12.3-19 34.3-46 83.6-46 61 0 106.8 39.8 106.8 125.4V448z"/>
            </svg>
          </a>
          <!-- Facebook -->
          <a href="#" class="circle" aria-label="Facebook">
            <svg viewBox="0 0 320 512" xmlns="http://www.w3.org/2000/svg">
              <path d="M279.14 288l14.22-92.66H196.6V129.77c0-25.35 12.42-50.06 52.24-50.06H293V6.26S269.91 0 247.13 0c-73.41 0-121.1 44.38-121.1 124.72V195.3H86.41V288h39.62v224h92.16V288z"/>
            </svg>
          </a>
        </div>

        <span class="login-subtext">or use your account</span>

        <!-- Rails Form -->
        <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: "login-form" }) do |form| %>
          <div class="form-group input-wrapper">
            <%= form.email_field :email, autofocus: true, autocomplete: "email", placeholder: "Email", class: "form-input #{'input-error' if resource.errors[:email].present?}" %>
          </div>
          <div class="form-group input-wrapper password-wrapper">
            <%= form.password_field :password, autocomplete: "current-password", placeholder: "Password", class: "form-input #{'input-error' if resource.errors[:password].present?}" %>
            <span class="toggle-password">&#128065;</span>
          </div>
          <% if devise_mapping.recoverable? %>
            <div class="form-group forgot-password">
              <%= link_to "Forgot your password?", new_password_path(resource_name) %>
            </div>
          <% end %>
          <div class="form-group">
            <%= form.submit "Sign In", class: "login-button" %>
          </div>
        <% end %>
      </div>

      <!-- Animated Text Section -->
      <div class="animated-text-section">
        <h3 class="animated-text-title">Welcome to RENATA</h3>
        <div class="animated-text-container">
          <div class="animated-text" id="animatedText"></div>
        </div>
      </div>
    </div>
  </div>
</main>

<!-- Footer Section -->
<footer class="app-footer">
  <div class="footer-container">
    <div class="footer-column footer-brand">
      <svg class="footer-name-svg" viewBox="0 0 420 48" xmlns="http://www.w3.org/2000/svg">
        <text x="0" y="34" font-family="Inter, system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial"
              font-weight="800" font-size="32" fill="#7af3ff" letter-spacing="2">
          RENATA
        </text>
      </svg>
      <p class="footer-tagline">Your futuristic AI companion — built for real emotions, real responses, and the real you.</p>
    </div>
    <div class="footer-column">
      <h4>Explore</h4>
      <ul class="footer-links">
        <li><a href="#">Features</a></li>
        <li><a href="#">Login</a></li>
        <li><a href="#">Signup</a></li>
        <li><a href="#">Privacy Policy</a></li>
      </ul>
    </div>
    <div class="footer-column">
      <h4>Contact</h4>
      <ul class="footer-contact">
        <li>hello@renata.ai</li>
        <li>+1-234-567-8910</li>
        <li>Future City, AIverse</li>
      </ul>
    </div>
  </div>
  <div class="footer-bottom">
    &copy; 2025 RENATA — Built with ❤️ & intelligence.
  </div>
</footer>


<div class="notification" id="notification"></div>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        // Dynamic hover effect that follows mouse - only for login container
        const authCard = document.getElementById('authCard');
        const hoverGlow = document.getElementById('hoverGlow');

        authCard.addEventListener('mousemove', function(e) {
            // Get the position of the auth card
            const rect = authCard.getBoundingClientRect();

            // Calculate the mouse position relative to the auth card
            const x = e.clientX - rect.left;
            const y = e.clientY - rect.top;

            // Position the glow at the mouse position
            hoverGlow.style.left = x + 'px';
            hoverGlow.style.top = y + 'px';
        });

        // Toggle password visibility
        const togglePassword = document.querySelector('.toggle-password');
        const passwordInput = document.querySelector('.password-wrapper input');

        togglePassword.addEventListener('click', function() {
            const type = passwordInput.getAttribute('type') === 'password' ? 'text' : 'password';
            passwordInput.setAttribute('type', type);
            this.textContent = type === 'password' ? '👁️' : '👁️‍🗨️';
        });

        // Form submission
        const loginForm = document.querySelector('.login-form');
        const notification = document.getElementById('notification');

        loginForm.addEventListener('submit', function(e) {
            e.preventDefault();

            // Get form values
            const email = document.querySelector('input[type="email"]').value;
            const password = document.querySelector('input[type="password"]').value;

            // Simple validation
            if (!email || !password) {
                showNotification('Please fill in all fields', 'error');
                return;
            }

            // Email validation
            const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            if (!emailRegex.test(email)) {
                showNotification('Please enter a valid email address', 'error');
                return;
            }

            // In a real application, you would send the login request to your server here
            // For demo purposes, we'll just show a success message
            showNotification('Login successful! Redirecting...', 'success');

            // Simulate redirect after successful login
            setTimeout(() => {
                // window.location.href = '/dashboard';
                console.log('Redirecting to dashboard...');
            }, 2000);
        });

        // Show notification
        function showNotification(message, type) {
            notification.textContent = message;
            notification.className = `notification ${type} show`;

            setTimeout(() => {
                notification.classList.remove('show');
            }, 5000);
        }

        // Animated text - character by character
        const animatedTextElement = document.getElementById('animatedText');
        const text = "Your journey to emotional wellbeing starts here";
        const characters = text.split('');

        // Function to animate text
        function animateText() {
            // Clear the animated text element
            animatedTextElement.innerHTML = '';

            // Create span elements for each character
            characters.forEach((char, index) => {
                const charSpan = document.createElement('span');
                charSpan.textContent = char;
                charSpan.classList.add('animated-char');
                charSpan.style.animationDelay = `${index * 0.1}s`; // Faster animation for characters
                animatedTextElement.appendChild(charSpan);
            });
        }

        // Initial animation
        animateText();

        // After the animation completes, restart it
        const totalAnimationTime = characters.length * 0.1 + 1; // Add 1 second for the last character to fully animate
        setInterval(() => {
            animateText();
        }, totalAnimationTime * 1000 + 2000); // Add 2 seconds before restarting
    });
</script>



