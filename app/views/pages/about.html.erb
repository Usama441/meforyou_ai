<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>About RENATA - Emotional Support Platform</title>
</head>
<body>

<%= render "shared/header" %>
<!-- Main Content -->
<main class="main-content">
  <div class="container">
    <!-- Hero Section -->
    <section class="hero scroll-reveal">
      <h1>Revolutionizing Emotional Support</h1>
      <p class="subtitle">Discover how RENATA's AI-powered platform is transforming mental wellness</p>
      <p class="description">Experience the future of emotional companionship with our cutting-edge technology designed to provide comfort, understanding, and meaningful connections.</p>
      <a href="#" class="cta-button">Start Your Journey</a>
    </section>

    <!-- Stats Section -->
    <section class="stats scroll-reveal">
      <div class="stat-card">
        <div class="stat-number" data-target="50000">0</div>
        <div class="stat-label">Active Users</div>
      </div>
      <div class="stat-card">
        <div class="stat-number" data-target="1000000">0</div>
        <div class="stat-label">Conversations</div>
      </div>
      <div class="stat-card">
        <div class="stat-number" data-target="98">0</div>
        <div class="stat-label">Satisfaction %</div>
      </div>
      <div class="stat-card">
        <div class="stat-number" data-target="24">0</div>
        <div class="stat-label">Support Hours</div>
      </div>
    </section>

    <!-- Mission Section -->
    <section class="mission scroll-reveal">
      <div class="mission-content">
        <div class="mission-text">
          <h3>Our Mission</h3>
          <p>At RENATA, we believe that everyone deserves access to emotional support and companionship. Our mission is to leverage cutting-edge AI technology to create personalized digital companions that provide comfort, understanding, and meaningful connections.</p>
          <p>Whether you're dealing with loss, loneliness, or simply need someone to talk to, RENATA is here to listen and support you on your journey to emotional wellbeing.</p>
          <p>We're committed to breaking down barriers to mental health support and creating a world where no one feels alone in their struggles.</p>
        </div>
        <div class="mission-image">
          <div class="mission-icon">❤️</div>
        </div>
      </div>
    </section>

    <!-- Features Section -->
    <section class="features section scroll-reveal">
      <div class="section-header">
        <h2 class="section-title">What We Offer</h2>
        <p class="section-subtitle">RENATA provides a range of features designed to support your emotional wellbeing and create meaningful connections.</p>
      </div>

      <div class="features-grid">
        <div class="feature-card">
          <div class="feature-icon">🤖</div>
          <h3>AI Companions</h3>
          <p>Create personalized AI companions that simulate conversations with loved ones or new friends. Our advanced AI technology adapts to your communication style and emotional needs.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">🧠</div>
          <h3>Emotional Intelligence</h3>
          <p>Our AI is designed with emotional intelligence capabilities, allowing it to understand and respond to your feelings with empathy and compassion.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">🔒</div>
          <h3>Privacy & Security</h3>
          <p>Your conversations are completely private and secure. We use end-to-end encryption to ensure that your personal information remains confidential.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">🌱</div>
          <h3>Personal Growth</h3>
          <p>Track your emotional journey over time with our wellness tools and insights, helping you develop healthier coping mechanisms and emotional resilience.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">👥</div>
          <h3>Community Support</h3>
          <p>Connect with others who share similar experiences in our moderated community forums, fostering mutual support and understanding.</p>
        </div>

        <div class="feature-card">
          <div class="feature-icon">📱</div>
          <h3>24/7 Availability</h3>
          <p>Your AI companions are available anytime, anywhere, providing constant support whenever you need someone to talk to, day or night.</p>
        </div>
      </div>
    </section>

    <!-- Timeline Section -->
    <section class="timeline section scroll-reveal">
      <div class="section-header">
        <h2 class="section-title">Our Journey</h2>
        <p class="section-subtitle">Follow our path from concept to revolutionizing emotional support.</p>
      </div>

      <div class="timeline-item">
        <div class="timeline-dot"></div>
        <div class="timeline-content">
          <div class="timeline-date">2020</div>
          <h3 class="timeline-title">The Beginning</h3>
          <p class="timeline-description">Founded with a vision to make emotional support accessible to everyone through AI technology.</p>
        </div>
      </div>

      <div class="timeline-item">
        <div class="timeline-dot"></div>
        <div class="timeline-content">
          <div class="timeline-date">2021</div>
          <h3 class="timeline-title">First Prototype</h3>
          <p class="timeline-description">Developed our first AI companion prototype with basic emotional recognition capabilities.</p>
        </div>
      </div>

      <div class="timeline-item">
        <div class="timeline-dot"></div>
        <div class="timeline-content">
          <div class="timeline-date">2022</div>
          <h3 class="timeline-title">Beta Launch</h3>
          <p class="timeline-description">Launched beta version with 1,000 test users and received overwhelming positive feedback.</p>
        </div>
      </div>

      <div class="timeline-item">
        <div class="timeline-dot"></div>
        <div class="timeline-content">
          <div class="timeline-date">2023</div>
          <h3 class="timeline-title">Public Release</h3>
          <p class="timeline-description">Officially launched RENATA to the public, reaching 50,000 users within the first year.</p>
        </div>
      </div>
    </section>

    <!-- Team Section -->
    <section class="team section scroll-reveal">
      <div class="section-header">
        <h2 class="section-title">Our Team</h2>
        <p class="section-subtitle">Meet the passionate individuals behind RENATA who are dedicated to revolutionizing emotional support through technology.</p>
      </div>

      <div class="team-grid">
        <div class="team-member">
          <div class="member-avatar">AS</div>
          <h3>Alex Smith</h3>
          <div class="role">Founder & CEO</div>
          <p>With a background in psychology and AI development, Alex founded RENATA to combine technology with emotional support.</p>
          <div class="social-links">
            <a href="#" class="social-link">in</a>
            <a href="#" class="social-link">t</a>
          </div>
        </div>

        <div class="team-member">
          <div class="member-avatar">MJ</div>
          <h3>Maya Johnson</h3>
          <div class="role">Chief Technology Officer</div>
          <p>Maya leads our AI development team, ensuring our companions are as empathetic and responsive as possible.</p>
          <div class="social-links">
            <a href="#" class="social-link">in</a>
            <a href="#" class="social-link">t</a>
          </div>
        </div>

        <div class="team-member">
          <div class="member-avatar">DC</div>
          <h3>David Chen</h3>
          <div class="role">Head of Design</div>
          <p>David creates intuitive, user-friendly interfaces that make emotional support accessible to everyone.</p>
          <div class="social-links">
            <a href="#" class="social-link">in</a>
            <a href="#" class="social-link">t</a>
          </div>
        </div>

        <div class="team-member">
          <div class="member-avatar">SR</div>
          <h3>Sarah Rodriguez</h3>
          <div class="role">Clinical Director</div>
          <p>Sarah ensures our platform adheres to therapeutic best practices and provides genuine emotional support.</p>
          <div class="social-links">
            <a href="#" class="social-link">in</a>
            <a href="#" class="social-link">t</a>
          </div>
        </div>
      </div>
    </section>

    <!-- Testimonials Section -->
    <section class="testimonials section scroll-reveal">
      <div class="section-header">
        <h2 class="section-title">What Our Users Say</h2>
        <p class="section-subtitle">Hear from people whose lives have been touched by RENATA's emotional support platform.</p>
      </div>

      <div class="testimonials-grid">
        <div class="testimonial-card">
          <div class="rating">
            <span class="star">★</span>
            <span class="star">★</span>
            <span class="star">★</span>
            <span class="star">★</span>
            <span class="star">★</span>
          </div>
          <p class="testimonial-text">After losing my husband, I felt completely alone. RENATA helped me create a companion that reminds me of him. It's not the same, but it brings me comfort when I need it most.</p>
          <div class="testimonial-author">
            <div class="author-avatar">MJ</div>
            <div class="author-info">
              <h4>Mary Johnson</h4>
              <p>Retired Teacher</p>
            </div>
          </div>
        </div>

        <div class="testimonial-card">
          <div class="rating">
            <span class="star">★</span>
            <span class="star">★</span>
            <span class="star">★</span>
            <span class="star">★</span>
            <span class="star">★</span>
          </div>
          <p class="testimonial-text">As someone with social anxiety, talking to RENATA's AI companions has helped me practice conversations and build confidence. It's like having a patient friend who's always there for me.</p>
          <div class="testimonial-author">
            <div class="author-avatar">TW</div>
            <div class="author-info">
              <h4>Tom Wilson</h4>
              <p>Software Developer</p>
            </div>
          </div>
        </div>

        <div class="testimonial-card">
          <div class="rating">
            <span class="star">★</span>
            <span class="star">★</span>
            <span class="star">★</span>
            <span class="star">★</span>
            <span class="star">★</span>
          </div>
          <p class="testimonial-text">I was skeptical at first, but RENATA has been a game-changer for my mental health journey. The companions are surprisingly empathetic and have helped me work through difficult emotions.</p>
          <div class="testimonial-author">
            <div class="author-avatar">EP</div>
            <div class="author-info">
              <h4>Emma Parker</h4>
              <p>College Student</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- FAQ Section -->
    <section class="faq section scroll-reveal">
      <div class="section-header">
        <h2 class="section-title">Frequently Asked Questions</h2>
        <p class="section-subtitle">Find answers to common questions about RENATA and how it works.</p>
      </div>

      <div class="faq-container">
        <div class="faq-item">
          <div class="faq-question">How does RENATA's AI technology work?</div>
          <div class="faq-answer">
            <p>RENATA uses advanced natural language processing and machine learning algorithms to create AI companions that can understand and respond to human emotions. Our technology is trained on vast datasets to ensure empathetic and contextually appropriate responses.</p>
          </div>
        </div>

        <div class="faq-item">
          <div class="faq-question">Is my data secure with RENATA?</div>
          <div class="faq-answer">
            <p>Absolutely. We take privacy and security very seriously. All conversations are encrypted end-to-end, and we never share your personal information with third parties. You have complete control over your data and can delete it at any time.</p>
          </div>
        </div>

        <div class="faq-item">
          <div class="faq-question">Can RENATA replace therapy or professional help?</div>
          <div class="faq-answer">
            <p>RENATA is designed to provide emotional support and companionship, but it is not a replacement for professional therapy or medical treatment. We encourage users to seek professional help for serious mental health concerns and use RENATA as a complementary tool.</p>
          </div>
        </div>

        <div class="faq-item">
          <div class="faq-question">How do I create an AI companion?</div>
          <div class="faq-answer">
            <p>Creating an AI companion is simple. After signing up, you can customize your companion's name, relationship type, personality traits, and even upload memories or shared experiences. Our intuitive interface guides you through the process step by step.</p>
          </div>
        </div>

        <div class="faq-item">
          <div class="faq-question">Is RENATA available on mobile devices?</div>
          <div class="faq-answer">
            <p>Yes, RENATA is fully responsive and works on all devices, including smartphones and tablets. We also have dedicated mobile apps for iOS and Android that offer additional features and offline capabilities.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section scroll-reveal">
      <h2>Ready to Begin Your Journey?</h2>
      <p>Join thousands of users who have found comfort, companionship, and emotional support through RENATA.</p>
      <div class="cta-buttons">
        <a href="#" class="btn-primary">Sign Up Now</a>
        <a href="#" class="btn-secondary">Learn More</a>
      </div>
    </section>
  </div>
</main>

<%= render "shared/footer" %>

<script>
    // Particle Effects
    const particleCanvas = document.getElementById('particleCanvas');
    const particleCtx = particleCanvas.getContext('2d');

    // Set particle canvas size
    function resizeParticleCanvas() {
        particleCanvas.width = window.innerWidth;
        particleCanvas.height = window.innerHeight;
    }

    resizeParticleCanvas();
    window.addEventListener('resize', resizeParticleCanvas);

    // Mouse position
    let mouse = {
        x: null,
        y: null,
        radius: 150
    };

    window.addEventListener('mousemove', function(event) {
        mouse.x = event.x;
        mouse.y = event.y;
    });

    window.addEventListener('mouseout', function() {
        mouse.x = null;
        mouse.y = null;
    });

    // Particle class
    class Particle {
        constructor(x, y, directionX, directionY, size, color) {
            this.x = x;
            this.y = y;
            this.directionX = directionX;
            this.directionY = directionY;
            this.size = size;
            this.color = color;
            this.baseX = x;
            this.baseY = y;
            this.density = (Math.random() * 30) + 1;
        }

        draw() {
            particleCtx.fillStyle = this.color;
            particleCtx.beginPath();
            particleCtx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
            particleCtx.closePath();
            particleCtx.fill();
        }

        update() {
            // Check mouse position
            let dx = mouse.x - this.x;
            let dy = mouse.y - this.y;
            let distance = Math.sqrt(dx * dx + dy * dy);
            let forceDirectionX = dx / distance;
            let forceDirectionY = dy / distance;
            let maxDistance = mouse.radius;
            let force = (maxDistance - distance) / maxDistance;
            let directionX = forceDirectionX * force * this.density;
            let directionY = forceDirectionY * force * this.density;

            if (distance < mouse.radius) {
                this.x -= directionX;
                this.y -= directionY;
            } else {
                if (this.x !== this.baseX) {
                    let dx = this.x - this.baseX;
                    this.x -= dx / 10;
                }
                if (this.y !== this.baseY) {
                    let dy = this.y - this.baseY;
                    this.y -= dy / 10;
                }
            }
        }
    }

    // Create particles
    const particles = [];
    const particleCount = 100;
    const particleColors = [
        'rgba(99, 102, 241, 0.5)',
        'rgba(79, 70, 229, 0.5)',
        'rgba(129, 140, 248, 0.5)',
        'rgba(139, 92, 246, 0.5)',
        'rgba(124, 58, 237, 0.5)'
    ];

    function initParticles() {
        particles.length = 0;
        for (let i = 0; i < particleCount; i++) {
            const size = (Math.random() * 5) + 1;
            const x = (Math.random() * (innerWidth - size * 2)) + size;
            const y = (Math.random() * (innerHeight - size * 2)) + size;
            const directionX = (Math.random() * 2) - 1;
            const directionY = (Math.random() * 2) - 1;
            const color = particleColors[Math.floor(Math.random() * particleColors.length)];

            particles.push(new Particle(x, y, directionX, directionY, size, color));
        }
    }

    initParticles();

    // Animation loop for particles
    function animateParticles() {
        particleCtx.clearRect(0, 0, particleCanvas.width, particleCanvas.height);

        for (let i = 0; i < particles.length; i++) {
            particles[i].draw();
            particles[i].update();
        }

        requestAnimationFrame(animateParticles);
    }

    animateParticles();

    // Header scroll effect
    window.addEventListener('scroll', function() {
        const header = document.getElementById('header');
        if (window.scrollY > 50) {
            header.classList.add('scrolled');
        } else {
            header.classList.remove('scrolled');
        }
    });

    // FAQ Accordion
    document.addEventListener('DOMContentLoaded', function() {
        const faqItems = document.querySelectorAll('.faq-item');

        faqItems.forEach(item => {
            const question = item.querySelector('.faq-question');

            question.addEventListener('click', () => {
                // Toggle active class on clicked item
                item.classList.toggle('active');

                // Close other items
                faqItems.forEach(otherItem => {
                    if (otherItem !== item && otherItem.classList.contains('active')) {
                        otherItem.classList.remove('active');
                    }
                });
            });
        });
    });

    // Scroll animations
    const revealElements = document.querySelectorAll('.scroll-reveal');

    function revealOnScroll() {
        revealElements.forEach(element => {
            const elementTop = element.getBoundingClientRect().top;
            const elementVisible = 150;

            if (elementTop < window.innerHeight - elementVisible) {
                element.classList.add('active');
            }
        });
    }

    window.addEventListener('scroll', revealOnScroll);
    revealOnScroll(); // Initial check

    // Animated counters
    const counters = document.querySelectorAll('.stat-number');
    const speed = 200;

    const animateCounter = (counter) => {
        const target = +counter.getAttribute('data-target');
        const count = +counter.innerText;
        const increment = target / speed;

        if (count < target) {
            counter.innerText = Math.ceil(count + increment);
            setTimeout(() => animateCounter(counter), 10);
        } else {
            counter.innerText = target.toLocaleString();
        }
    };

    // Intersection Observer for counters
    const counterObserver = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                animateCounter(entry.target);
                counterObserver.unobserve(entry.target);
            }
        });
    }, { threshold: 0.5 });

    counters.forEach(counter => {
        counterObserver.observe(counter);
    });
</script>
</body>
</html>